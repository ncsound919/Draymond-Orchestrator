# scipy is intentionally absent: codex_metrics imports the scipy-free
# analytics submodules (ter_engine, four_factors) directly, so bbtech's
# analytics/__init__ chain (spatial_metrics, optimization_engine) is never
# pulled in.
numpy>=1.20
requests>=2.25
sympy>=1.9
