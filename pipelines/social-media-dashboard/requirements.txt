# ============================================================================
# social-media-dashboard pipeline — consolidated dependencies
# ============================================================================
# One requirements file for the whole marketing pipeline. Each stage's real
# manifest is included with `-r` so nothing drifts from the folded tools.
# Install:  pip install -r pipelines/social-media-dashboard/requirements.txt
# ============================================================================

# overlay-marketing-voice / scheduler / format / tracker run via npx tsx
# (no Python deps) — they resolve at runtime through the shared marketing/index.ts.

# marketing-tool — asset automation engine
-r ../../../04_Integrations/integrations/Marketing-Tool/requirements.txt
